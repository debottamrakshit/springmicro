spring:
  cloud:
    config:
      name: eureka-server-1
      uri: http://localhost:8888
      failFast: true
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 15000
        maxAttempts: 20